"""Bring-up and diagnostic entrypoints (docs/deploy_refactor_plan.md §1):
the check rung ladder, the replay tools, and the wire diagnostics. Every
documented `python -m ego2g1.deploy.<name>` invocation still works via
the top-level shims."""
